# Copy to .env or export in your shell. .env is git-ignored.
# Required: every run calls the real Anthropic API and fails fast without this key.
ANTHROPIC_API_KEY=

# Test harness only: set to 1 when you need to prove a missing-key path ignores local .env.
PYTHON_DOTENV_DISABLED=
