# Core linter: stdlib only - nothing to install.
#
# Optional extras:
#   anthropic==0.109.0   # --judge: Claude rewrites the worst tool contract
#   mcp==1.28.0          # lint a FastMCP server .py directly instead of a tools JSON
