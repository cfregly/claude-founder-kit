# Copy to .env and fill in. The real .env is git-ignored.
# Required: every demo in this kit makes a real Claude API call.
ANTHROPIC_API_KEY=

# Optional: launch can push events to a product-analytics backend.
# One of: local (default), posthog, statsig, amplitude.
ACTIVATION_BACKEND=local
# STATSIG_API_KEY=
# POSTHOG_API_KEY=
