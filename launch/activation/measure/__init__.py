"""Step 2, measure: turn the cohort into an attributable activation readout.

The deck draws the loop; this scores a real cohort that ran through it. Funnel,
activation rate, time-to-second-build, the leaky-bucket and engagement flags, and
the product-qualified accounts ready for a named GTM owner. Deterministic, so the
same cohort gives the same readout every run.
"""
